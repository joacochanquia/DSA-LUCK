version: '3'

services:
  db:
    container_name: mysqldb-ctf
    image: mysql:5.7
    restart: always
    environment:
        MYSQL_RANDOM_ROOT_PASSWORD: "yes"
        MYSQL_DATABASE: "ctf"
        MYSQL_USER: "user"
        MYSQL_PASSWORD: "password"
    volumes:
      - "./database/:/docker-entrypoint-initdb.d"

  pythonctfix:
    container_name: python-ctfix
    image: ctf-project-fix
    restart: always
    links:
      - db
    depends_on:
      - db
    volumes:
      - "./www/:/www/"
    ports:
      - 50001:8888